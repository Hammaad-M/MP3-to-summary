# MP3 to Summary

## Installation

Run `pip install -r requirements.txt` in root folder.

## Usage

Change `audio_path` to match your audio input file. Run `python main.py` to launch program. Summary is stored to `transcription.txt`, updated after processing each 30 second chunk.
